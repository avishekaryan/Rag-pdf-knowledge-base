# RAG PDF Knowledge Base

A Python-based document ingestion and indexing system that processes PDF files, generates vector embeddings, and stores them in Pinecone for Retrieval-Augmented Generation (RAG) applications.

## Features
- PDF document ingestion
- Text chunking
- Embedding generation
- Pinecone vector indexing
- Semantic document retrieval support
